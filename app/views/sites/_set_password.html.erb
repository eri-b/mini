<%= form_for @site, url: site_pass_path(@site.name), method: :patch do |f| %>

  <%= f.label :password %>
  <%= f.password_field :password, class: "input is-small" %>
  <%= f.submit "Add password", class: "button is-small" %>

<% end %>
