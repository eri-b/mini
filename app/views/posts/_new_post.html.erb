<%= form_for Post.new, :html => {:class => ''} do |f| %>

  <%= f.text_area :body , class: "textarea is-small", rows:3 %>
  <%= f.hidden_field :site, value: @site.name %>
  <%= f.submit "Post", class: "button is-light post" %>

<% end %>
